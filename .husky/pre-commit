npm run verify-version
npx lint-staged

# Secret sync is opt-in to avoid blocking commits when prerequisites aren't met
# Enable by setting: export SYNC_SECRETS=1
# Auto-skipped in CI environments
if [ "$SYNC_SECRETS" = "1" ] && [ -z "$CI" ]; then
  echo "📦 .env Syncing secrets to GitHub..."
  echo ""
  node scripts/sync-secrets.js
  echo ""
fi
