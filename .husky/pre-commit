npm run verify-version
npx lint-staged

# Skip secret sync if SKIP_SECRET_SYNC is set (useful for local development)
if [ -z "$SKIP_SECRET_SYNC" ]; then
  echo "📦 .env Syncing secrets to GitHub..."
  echo ""
  node scripts/sync-secrets.js
  echo ""
fi