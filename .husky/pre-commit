npm run verify-version
npx lint-staged

# Secret sync runs by default to keep GitHub secrets in sync with .env
# Skip by setting: export SKIP_SECRET_SYNC=1
# Auto-skipped in CI environments
if [ -z "$SKIP_SECRET_SYNC" ] && [ -z "$CI" ]; then
  echo "📦 .env Syncing secrets to GitHub..."
  echo ""
  node scripts/sync-secrets.js
  echo ""
fi
